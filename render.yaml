services:
  - type: web
    name: it-asset-manager
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:app"
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0


